This is a project for 463 which is a basic client/server messaging system.

To run, do the following:

On one terminal, run server.py

This project uses port 8080, ensure the port on your device is free or else you will get errors.

On as many other terminals as you want, run client.py which act as clients.
